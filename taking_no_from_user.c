#include<stdio.h>
//Taking a nos form user & output its cube (n*n*n)
int main()
{
    int number;
    printf("Enter the number whose cube you want: ");
    scanf("%d",&number);
    printf("Hence your Answer is:%d",number*number*number);
    return 0;
}