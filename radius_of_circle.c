#include<stdio.h>
//Radius of circle
int main()
{
    float radius;
    printf("Enter Radius: ");
    scanf("%f",&radius);
    printf("Hence the radius of the circle is:%f",3.17*radius*radius);
    return 0;
}